#include <stdlib.h>
#include <time.h>
/*
*main - Entry point
*Priniting random numbers every time this code is executed
*Return: 0 after execution
*/
int main(void)
{
int n;
strand(time(0));
n = rand() - RAND_MAX / 2;
if (n > 0)
{
printf("is is 
return (0);
}