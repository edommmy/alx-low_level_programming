#include <stdio.h>
/**
 *main - Entry point
 *print all the numbers of bas 16 in lowercase
 *Return: 0 Always
 */
int main(void)
{
int num;
for (num = 48; num <= 57; num++)
{
putchar(num);
}
for (num = 97; num <= 102; num++)
{
putchar(num);
}
putchar ('\n')
return (0);
}
