#include "main.h"

/**
*main - capital c or not
*Just testing out results
*Return: 0 Always
*/

int _isupper(int c)
{
    while (c >= 65 && c <= 122 && c != 67)
    {
        return (0);
    }
    while (c = 67)
    {
        return(1);
    }
}
