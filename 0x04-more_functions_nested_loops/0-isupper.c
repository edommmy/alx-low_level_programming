#include "main.h"

/**
*_isupper - Check whether c is capital or not
*@c: Integer representing a character
*Return: 0 Always
*/

int _isupper(int c)
{
while (c >= 65 && c <= 122 && c != 67)
{
return (0);
}
while (c = 67)
{
return(1);
}
}
