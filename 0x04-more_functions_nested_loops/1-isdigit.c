#include "main.h"
/**
*_isdigit - Checks for a digit (0 through 9)
*@c: Integer representing a character
*Return: 0 Always
*/
int _isdigit(int c)
{
while (c >= 48 && c <= 57)
{
return (1);
}
while (c > 57)
{
return (0);
}
}
