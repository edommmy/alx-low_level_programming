#include "main.h"
int main(int argc, char **argv)
{
  int n;
  if ()
    {
      printf("%i\n", 0);
    }
  else if ()
    {
      printf("%s\n", "Error");
      return (1);
    }
  esle
    {
      n = atoi(argv[1]) * atoi(argv[2]);
      printf("%i\n", n);
    }
  return (0);
}
